<Window x:Class="ExcelSheetsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wells"
        mc:Ignorable="d"
        Title="Hojas del archivo Excel" WindowStartupLocation="CenterScreen" Style="{StaticResource DialogStyle}" MinWidth="350">
    <StackPanel Margin="2">
        <DockPanel>
            <TextBlock Text="Seleccione una hoja para importar: " Style="{StaticResource MarginStyle}"/>
            <ComboBox Name="SheetsComoboBox" IsEditable="False" Style="{StaticResource MarginStyle}"
                      SelectedIndex="{Binding SelectedSheet}"/>
        </DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Name="OkButtom" Content="Aceptar" Style="{StaticResource ButtonStyle}"/>
            <Button Name="CanelButtom" Content="Cancelar" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </StackPanel>
</Window>
