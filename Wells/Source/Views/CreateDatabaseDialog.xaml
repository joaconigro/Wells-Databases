<Window x:Class="CreateDatabaseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wells"
        mc:Ignorable="d"
        Title="Crear base de datos" Height="Auto" Width="500" ResizeMode="NoResize" 
        ShowInTaskbar="False" SizeToContent="Height" WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow" Topmost="True">
    <Grid VerticalAlignment="Center" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Nombre de la base de datos: " Margin="2" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Text="Carpeta contenedora: " Margin="2" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox Name="DatabaseNameTextBox" Grid.Row="0" Grid.Column="1" Margin="2" Text="{Binding DatabaseName}"/>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="DatabasePathTextBox" Grid.Column="0" Margin="2" Text="{Binding DatabasePath}"/>
            <Button Name="ChooseDatabasePathButton" Content=" Elegir carpeta... " Grid.Column="1" Margin="2"/>
        </Grid>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content=" Aceptar " Grid.Column="0" Margin="2" Padding="2"/>
            <Button Name="CancelButton" Content=" Cancelar " Grid.Column="1" Margin="2" Padding="2"/>
        </StackPanel>
    </Grid>
</Window>
