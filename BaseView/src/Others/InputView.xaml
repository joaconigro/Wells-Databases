<Window x:Class="Wells.BaseView.InputView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Style="{StaticResource DialogStyle}" WindowStartupLocation="CenterOwner" Height="200" Width="400"
        ContentRendered="Window_ContentRendered">
    <Grid Style="{StaticResource MarginStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Message}" Style="{StaticResource MarginStyle}"/>
        <TextBox x:Name="InputTextBox" Text="{Binding Input}" Style="{StaticResource MarginStyle}" Grid.Row="1" VerticalAlignment="Top"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" VerticalAlignment="Bottom">
            <Button x:Name="OkButton" Content="Aceptar" Style="{StaticResource ButtonStyle}" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancelar" Style="{StaticResource ButtonStyle}" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
